"""Trivial helpers exercised by tests/test_calc.py."""

from __future__ import annotations


def add(a: int, b: int) -> int:
    return a + b


def greet(name: str) -> str:
    return f"hello, {name}"
