{
  "name": "ci-templates-selftest-node-app",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal Node.js project used by .github/workflows/selftest.yml to dog-food reusable-ci.yml. Zero external dependencies on purpose.",
  "license": "MIT",
  "type": "commonjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "node --check src/index.js",
    "build": "node scripts/build.js",
    "test": "node --test --test-reporter=spec"
  }
}
